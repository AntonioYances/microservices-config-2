server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/AntonioYances/microservices-config
          search-paths: resources


application:
  name: config-server

